# EV4 Builder

The planned workflow is CE input to Builder output to EV4 Project Gate to Responsive input. The verifier is not implemented yet.

Builder owns execution protocols and build evidence. Project Gate checks incoming and outgoing handoffs and does not replace Builder contracts. See `README.md`.
